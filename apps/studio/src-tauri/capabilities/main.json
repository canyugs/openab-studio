{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "main",
  "description": "Minimal capability for the bootstrap window.",
  "windows": ["main"],
  "permissions": ["allow-workspace-bootstrap"]
}
